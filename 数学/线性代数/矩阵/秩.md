# 秩


在 $m \times n$ 矩阵 $\boldsymbol{A}$ 中，任取 $k$ 行与 $k$ 列 $(k \leqslant m, k \leqslant n)$，位于这些行列交叉处的 $k^2$ 个元素，不改变它们在 $\boldsymbol{A}$ 中所处的位置次序而得的 $k$ 阶行列式，称为矩阵 $\boldsymbol{A}$ 的 $k$ 阶**子式**.



- 设在矩阵 $\boldsymbol{A}$ 中有一个不等于 0 的 $r$ 阶子式 $D$
- 且所有 $r+1$ 阶子式（如果存在的话）全等于 0
- 那么 $D$ 称为矩阵 $\boldsymbol{A}$ 的*最高阶非零子式*
	- 数 $r$ 称为矩阵 $\boldsymbol{A}$ 的**秩**
	- 记作 $R(\boldsymbol{A})$
- 并规定零矩阵的秩等于0
## 性质


由于 $R(A)$ 是 $A$ 的非零子式的最高阶数，因此，若矩阵 $A$ 中有某个 $s$ 阶子式不为 $0$，则 $R(A) \geqslant s$；若 $A$ 中所有 $t$ 阶子式全为 $0$，则 $R(A) < t$ ．


显然，若 $\boldsymbol{A}$ 为 $m \times n$ 矩阵，则 $0 \leqslant R(\boldsymbol{A}) \leqslant \min\{m, n\}$ ．  

由于行列式与其转置行列式相等，因此 $\boldsymbol{A}^{\mathrm{T}}$ 的子式与 $\boldsymbol{A}$ 的子式对应相等，从而 $R(\boldsymbol{A}^{\mathrm{T}}) = R(\boldsymbol{A})=R(A^{T}A)=R(A^{T}A)$ ．  


对于 $n$ 阶矩阵 $\boldsymbol{A}$，由于 $\boldsymbol{A}$ 的 $n$ 阶子式只有一个 $|\boldsymbol{A}|$，故当 $|\boldsymbol{A}| \neq 0$ 时 $R(\boldsymbol{A}) = n$，当 $|\boldsymbol{A}| = 0$ 时 $R(\boldsymbol{A}) < n$ ．可见可逆矩阵的秩等于矩阵的阶数，不可逆矩阵的秩小于矩阵的阶数．


因此，可逆矩阵又称满秩矩阵，不可逆矩阵(奇异矩阵)又称降秩矩阵．

### 性质 5
⑤ $\max\{R(A),R(B)\} \leqslant R(A,B) \leqslant R(A) + R(B)$，  
- 特别地，当 $B = b$ 为非零列向量时，有$R(A) \leqslant R(A,b) \leqslant R(A) + 1$ .  

证 因为 $A$ 的最高阶非零子式总是 $(A,B)$ 的非零子式，所以 $R(A) \leqslant R(A,B)$ . 同理有 $R(B) \leqslant R(A,B)$ . 两式合起来，即为  
$\max\{R(A),R(B)\} \leqslant R(A,B)$ .  
设 $R(A) = r, R(B) = t$ . 把 $A^{\mathrm{T}}$ 和 $B^{\mathrm{T}}$ 分别作初等行变换化为行阶梯形矩阵 $\widetilde{A}$ 和 $\widetilde{B}$ . 因由性质 2，$R(A^{\mathrm{T}}) = r, R(B^{\mathrm{T}}) = t$，故 $\widetilde{A}$ 和 $\widetilde{B}$ 中分别含 $r$ 个和 $t$ 个非零行，从而 $\begin{pmatrix} \widetilde{A} \\ \widetilde{B} \end{pmatrix}$ 中只含 $r + t$ 个非零行，并且 $\begin{pmatrix} A^{\mathrm{T}} \\ B^{\mathrm{T}} \end{pmatrix} \stackrel{r}{\sim} \begin{pmatrix} \widetilde{A} \\ \widetilde{B} \end{pmatrix}$ . 于是  
$R(A,B) = R\begin{pmatrix} A^{\mathrm{T}} \\ B^{\mathrm{T}} \end{pmatrix}^{\mathrm{T}} = R\begin{pmatrix} A^{\mathrm{T}} \\ B^{\mathrm{T}} \end{pmatrix} = R\begin{pmatrix} \widetilde{A} \\ \widetilde{B} \end{pmatrix} \leqslant r + t = R(A) + R(B)$ .  证毕  

### 性质 6

⑥ $R(A + B) \leqslant R(A) + R(B)$ .  
- 无妨设 $A, B$ 为 $m \times n$ 矩阵. 对矩阵 $\begin{pmatrix} A + B \\ B \end{pmatrix}$ 作初等行变换 $r_i - r_{n + i}$（$i = 1, 2, \cdots, n$）即得  
$\begin{pmatrix} A + B \\ B \end{pmatrix} \stackrel{r}{\sim} \begin{pmatrix} A \\ B \end{pmatrix}$，  于是$R(A + B) \leqslant R\begin{pmatrix} A + B \\ B \end{pmatrix} = R\begin{pmatrix} A \\ B \end{pmatrix}$ $= R\begin{pmatrix} A^{\mathrm{T}}, B^{\mathrm{T}} \end{pmatrix}^{\mathrm{T}}$ $= R(A^{\mathrm{T}}, B^{\mathrm{T}}) \stackrel{\text{由⑤}}{\leqslant} R(A^{\mathrm{T}}) + R(B^{\mathrm{T}})$  $= R(A) + R(B)$ .  证毕  

### 性质 7

$R(AB)\leq \max{(R(A),R(B))}$


定理 2 若 $\boldsymbol{A} \sim \boldsymbol{B}$，则 $R(\boldsymbol{A}) = R(\boldsymbol{B})$ ．  
- 由引理，只须证明 $\boldsymbol{A}$ 经初等列变换变成 $\boldsymbol{B}$ 的情形，这时 $\boldsymbol{A}^{\mathrm{T}}$ 经初等行变换变为 $\boldsymbol{B}^{\mathrm{T}}$，由引理知 $R(\boldsymbol{A}^{\mathrm{T}}) = R(\boldsymbol{B}^{\mathrm{T}})$，又 $R(\boldsymbol{A}) = R(\boldsymbol{A}^{\mathrm{T}})$，$R(\boldsymbol{B}) = R(\boldsymbol{B}^{\mathrm{T}})$，因此 $R(\boldsymbol{A}) = R(\boldsymbol{B})$ ．  
- 总之，若 $\boldsymbol{A}$ 经有限次初等变换变为 $\boldsymbol{B}$（即 $\boldsymbol{A} \sim \boldsymbol{B}$），则 $R(\boldsymbol{A}) = R(\boldsymbol{B})$ ． 证毕  

由于 $\boldsymbol{A} \sim \boldsymbol{B}$ 的充分必要条件是有可逆矩阵 $\boldsymbol{P}$、$\boldsymbol{Q}$，使 $\boldsymbol{P}\boldsymbol{A}\boldsymbol{Q} = \boldsymbol{B}$，因此可得  

推论 若可逆矩阵 $\boldsymbol{P}$、$\boldsymbol{Q}$ 使 $\boldsymbol{P}\boldsymbol{A}\boldsymbol{Q} = \boldsymbol{B}$，则 $R(\boldsymbol{A}) = R(\boldsymbol{B})$ ．


### 性质 8

⑧ 若 $A_{m \times n}B_{n \times l} = O$，则 $R(A) + R(B) \leqslant n$


## 计算

### 求矩阵的秩

对矩阵进行初等变换为阶梯形矩阵，非零行的个数就为秩

例 18 求$\begin{pmatrix}1 & -1 & 2 & 1 & 0 \\2 & -2 & 4 & 2 & 0 \\3 & 0 & 6 & -1 & 1 \\0 & 2 & 0 & 0 & 1\end{pmatrix}$，求 A 的秩，并求一个最高阶的非零子式
- $\begin{pmatrix}1 & -1 & 2 & 1 & 0 \\2 & -2 & 4 & 2 & 0 \\3 & 0 & 6 & -1 & 1 \\0 & 2 & 0 & 0 & 1\end{pmatrix}$$r_{2}-2r_{1}r_{3}-3r_{1}\begin{pmatrix}1 & -1  & 2 &  1 & 0 \\0 & 0 &  0 & 0 & 0 \\0 & 3 & 0 & -4 & 1 \\0 & 3 & 0 & 0 & 1 \\\end{pmatrix}$
- $r_{2}\leftrightarrow r_{4}\begin{pmatrix}1 & -1 & 2 & 1 & 0 \\0 & 3 & 0 & 0 & 1 \\0 & 2 & 0 & -4 & 1 \\0 & 0 & 0 & 0 & 0\end{pmatrix}$
- $r_{3}-r_{2}\begin{pmatrix}1 & -1 & 2 & 1 & 0 \\0 & 3 & 0 & 0 & 1 \\0 & 0 & 0 & -4 & 0 \\0 & 0 & 0 & 0 & 0\end{pmatrix}$
- 非零行的个数为 3，秩为 3


例 19：$\boldsymbol{A}=\begin{pmatrix} 1 & -1 & 1 & 2 \\ 3 & \lambda & -1 & 2 \\ 5 & 3 & \mu & 6 \end{pmatrix}$，$r(\boldsymbol{A})=2$  
- $\xrightarrow{R_3-3R_1}\begin{pmatrix} 1 & -1 & 1 & 2 \\ 0 & \lambda + 3 & -4 & -4 \\ 0 & 8 & \mu - 5 & -4 \end{pmatrix}$
- $1^{\circ}\frac{\lambda + 3}{8}=\frac{-4}{\mu - 5}=\frac{-4}{-4}$
- $2^{\circ}\xrightarrow{\ R_3-R_2}\begin{pmatrix} 1 & -1 & 1 & 2 \\ 0 & \lambda + 3 & -4 & -4 \\ 0 & 5 - \lambda & \mu - 1 & 0 \end{pmatrix}$
- $\begin{cases} 5 - \lambda = 0 \\ \mu - 1 = 0 \end{cases}$
