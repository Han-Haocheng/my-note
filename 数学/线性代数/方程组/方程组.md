# 方程组


方程组



设 $F$ 为域，给定矩阵 $A \in F^{m \times n}$ 及非零列向量 $b \in F^m$（即 $b \neq \mathbf{0}$）。含有 $n$ 个未知量、$m$ 个方程的线性方程组
**代数形式：**

其中常数项 $b_i \in F$ 不全为零（即 $\exists i \in \{1, 2, \dots, m\}, b_i \neq 0$），称为**非齐次线性方程组**。
**矩阵形式：**
$$ Ax = b $$
其中 $A = (a_{ij})_{m \times n}$ 为系数矩阵，$(A,B)$ 为增广矩阵，$x = (x_1, x_2, \dots, x_n)^T \in F^n$ 为未知数向量，$b = (b_1, b_2, \dots, b_m)^T \in F^m$ 为非零常数项向量。


## 基础解系

齐次线性方程组 $Ax = \mathbf{0}$ 的解空间 $S$ 的一个基，称为该方程组的一个**基础解系**。

设 $\xi_1, \xi_2, \dots, \xi_t \in S$ 是 $Ax = \mathbf{0}$ 的解向量。若 $\xi_1, \xi_2, \dots, \xi_t$ 满足：
1. **线性无关**：$\xi_1, \xi_2, \dots, \xi_t$ 线性无关；
2. **极大性（生成性）**：方程组的任意一个解 $\xi$ 均可由 $\xi_1, \xi_2, \dots, \xi_t$ 线性表出，即存在 $c_1, c_2, \dots, c_t \in F$，使得 $\xi = c_1\xi_1 + c_2\xi_2 + \dots + c_t\xi_t$；
则称向量组 $\xi_1, \xi_2, \dots, \xi_t$ 为该方程组的一个**基础解系**。

*(注：若 $r = n$，解空间 $S = \{\mathbf{0}\}$，此时不存在基础解系。)*

### 性质

设 $\xi_1, \xi_2, \dots, \xi_{n-r}$ 为 $Ax = \mathbf{0}$ 的一个基础解系，则具有以下性质：

**向量个数确定性**：基础解系中所含向量的个数恒等于 $n - r(A)$，该数称为解空间 $S$ 的维数，记作 $\dim(S) = n - r(A)$。

**不唯一性**：若 $n - r(A) > 0$，基础解系不唯一。解空间 $S$ 的任意一组基均可作为基础解系，且任意两个不同的基础解系是等价的。

**解的结构性（齐次通解）**：方程组 $Ax = \mathbf{0}$ 的任意解（通解）均可表示为基础解系的线性组合，即解集为：
$$ S = \{ k_1\xi_1 + k_2\xi_2 + \dots + k_{n-r}\xi_{n-r} \mid k_i \in F, i = 1, 2, \dots, n-r \} $$


**延拓性质（与非齐次方程组的关系）**：设 $\eta^*$ 是非齐次线性方程组 $Ax = b$ 的一个特解（即 $A\eta^* = b$），则非齐次方程组的解集可表示为：
$$ \{ x \mid Ax = b \} = \{ \eta^* + k_1\xi_1 + k_2\xi_2 + \dots + k_{n-r}\xi_{n-r} \mid k_i \in F \} $$
即：**非齐次通解 = 非齐次特解 + 齐次通解**。

## 定理

当齐次方程组 $Ax=0$ 的系数矩阵 $r(A)<n$ 时，即有非零解时，基础解系必存在

## 计算
### 解的判定

齐次
- $r(A)<n \Leftrightarrow Ax=0$ 有非零解
- $r(A)=n \Leftrightarrow Ax=0$ 只有零解
- 当 $\begin{vmatrix}A \end{vmatrix}=0 \Leftrightarrow$

### 求解方程组

1. 列出系数增广矩阵 $A$ 化简成最简形 $C$
2. 由 $C$ 写出对应的齐次方程组
	- $C$ 中主元 $1$ 对应未知量作为非自由未知量
	- 其余的未知量作为自由未知量
	- 令自由未知量作为任意常数 $C_{1},C_{2},\dots,C_{n-r}$，移项

### 基础解系中含有向量数量

$A_{m\times n}x=0$，且 $r(A)=r<n$，则 $Ax=0$ 基础解系中含有 $n-r(A)$ 个向量
