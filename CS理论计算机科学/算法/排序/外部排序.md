# 外部排序


**外部排序**(外排序)：：对大文件的排序，当数据量很大时，内存无法容纳全部的待排序数据，部分数据需要存储在外存上。在排序过程中，需要多次进行内外存之间的数据交换。

- [[多路平衡归并]]、[[胜败者树]]、[[置换选择排序]]、[[最佳归并树]]

## 基本步骤
1. 按可用内存大小，利用内部排序方法，构造若干个记录的有序子序列写入外存，通常称这些记录的有序子序列为归并段；
2. 通过归并，逐步扩大（记录的）有序子序列的长度，直至外存中整个记录序列按关键字有序为止

