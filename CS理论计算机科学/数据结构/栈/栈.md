# 栈

- 栈和队列时限定插入和删除，只能在端点进行的*线性表*

![[20250421161800.svg]]

**栈(stack)**：是一个特殊的线性表，是限定在一端(通常是表尾)进行插入和删除操作的线性表，是*后进先出*的线性表
- **后进先出(Last In First Out，LIFO)**：率先使用最后插入的数据
- **栈顶(Top)**：表尾($a_{n}$端)
- **栈底(Base)**：表头($a_{1}$端)，
- **入栈(Push)**：将元素插入的到*栈顶*的操作
- **出栈(Pop)**：从*栈顶*删除最后一个元素的操作


## 抽象数据类型
### 数据对象
数据对象：$D=\{a_i | a_i \in ElemSet, i\in Z^+\}$
### 数据关系

数据关系：$R1=\{<a_i-1,a_i>|a_i-1,a_i\in D,i=2,\dots,n\}$
- 约定$a_{n}$端为栈顶，$a_{1}$端为栈底。

### 基本操作
- 初始化操作：`InitStack(&S)`
	- 操作结果：构造一个空栈$S$
- 销毁栈操作：`DestroyStack(&S)`
	- 初始条件：栈$S$已存在
	- 操作结果：栈$S$被销毁
- 判断$S$是否为空栈：`StackEmpty(S)`
	- 初始条件：栈$S$已存在
	- 操作结果：若栈$S$为空栈，返回`TRUE`，否则 `FALSE`
- 求栈的长度`StackLength(S)`
	- 初始条件：栈$S$已存在
	- 操作结果：返回$S$的元素个数，即栈的长度
- 获取栈顶元素：`GetTop(S,&e)`
	- 初始条件：栈$S$已经存在，且非空
	- 操作结果：用$e$返回$S$的栈顶元素
- 栈置空操作：`ClearStack(&S)`
	- 初始条件：栈$S$已存在
	- 操作结果：将$S$清为空栈
- 入栈操作：`Push(&S,e)`
	- 初始条件：栈$S$已经存在
	- 操作结果：插入元素$e$为新的栈顶元素
- 出栈操作：`Pop(&S,&e)`
	- 初始条件：栈$S$已存在且非空
	- 操作结果：删除$S$的栈顶元素$a_{n}$，并用$e$返回其值


存储方式

[[顺序栈]]

共享栈：用一个数组来实现两个栈，一个栈位于起始，另一个位于末尾
```c
typedef struct
{
	SElemType data[MAXSIZE];
	int top1,top2;
}DStack;
```

链栈：用链式存储结构的栈，通常不带头节点的单链表表示
```c
typedef struct StackNode{
	SElemType data;
	struct StackNode *next;
}StackNode,*LlinkStack;
```

## 5 基本操作
### 5.1 栈初始化

```C++
Status InitStack(SqStack&S)
{
	S.base = (SElemType*)malloc(MAXSIZE*sizeof(SElemType));
	if(!S.base) exit(OVERFLOW); // 内存分配失败
	S.top = S.base;
	S.stacksize = MAXSIZE;
	return OK;
};
```

链栈

```c
Status InitStack(LinkStack&S)
{
	S=NULL;
	return OK;
}
```

### 5.2 栈判空

```c++
Status StackEmpty(SqStack S){
	return S.top == S.base;
}

Status StackEmpty(LinkStack &S){
	return S == NULL;
}
```

### 5.3 栈获取长度

```c++
int StackLength(SqStack S){
	return S.top - S.base;
}
```


### 5.4 栈清空

```c++
Status ClearStack(SqStack S){
	if(S.base) S.top = S.base;
	return OK;
}
```

### 5.5 栈销毁

```c++
Status DestroyStack(SqStack &S){
	if(!S.base)return OK;
	delete S.base;
	S.stacksize = 0;
	S.base = S.top = NULL;
	return OK;
}
```

### 5.6 栈入栈

顺序栈
```C++
Status Push(SqStack &S,SElemType e){
	if(S.top - S.base == S.stacksize)
	{
		ElemType* newBase =
		 (ElemType*)remalloc(S.base,S.stacksize*2*sizeof(ElemType));
		if(!newBase)return STACK_FULL_ERROR;
		S.base = newBase;
		s.top =S.base+S.stacksize;
		S.stacksize*=2;
	}
	*S.top = e;
	++S.top;
	return OK;
}
```

链栈
```c
Status Push(LinkStack &s, SElemType e){
	struct StackNode*p;
	p = (struct StackNode*)malloc(sizeof(struct StackNode));
	p->data=e;
	p->next=s;
	S = p;
	return OK
}
```

### 5.7 栈出栈

顺序栈出栈
```C++
Status Pop(SqStack &S, SElemType &e){
	if(S.top == S.base)return ERROR;
	e = *(--S.top);
	return OK;
}
```

链栈出栈

```c
status Pop(LinkStack &s,SElemType &e){
	struct StackNode*p;
	if(s==NULL)return ERROR;
	e=s->data;
	p=s;
	s= s->next;
	free(p);
	return OK;
}
```

### 5.8 栈获取栈顶元素

顺序栈
```c
Status GetTop(SqStack S, ElemType&e){
	if(S.top == S.base) return ERROR;
	e=*(S.top-1);
	return OK
}
```

链栈
```c++
SElemType GetTop(LinkStack S){
	if(S==NULL) exit();
	return S->data;
}
```