# 矩阵

矩阵Matrix：在科学与工程计算问题中，是一种常用的数学对象。
- 在采用高级语言编写程序时，简单而又自然的方法，就是将一个矩阵描述为一个二维数组。矩阵在这种存储表示之下，可以对其数据元素进行随机存取，各种矩阵运算也非常简单。
- 在矩阵中，若非零数据元素呈某种规律分布或矩阵中出现大量零数据元素，如果仍采用常规的矩阵存储方法，将会占用许多空间去存储重复的非零数据元素或零数据元素，对于高阶矩阵来说，这样会造成极大的浪费。因此，为了节省存储空间，可以对此类矩阵进行压缩存储。
- 常见的特殊矩阵：对称矩阵、三角矩阵和对角矩阵等，都是方阵，即行数和列数相同。


## 压缩存储原则
- 为多个值相同的非零数据元素分配一个存储空间；2
- 不为零数据元素分配存储空间

假如值相同的非零数据元素或者零数据元素在矩阵中的分布有一定规律$a_{ij}=a_{ji},(0\leq 1,h\leq n-1)$
- 则称此类矩阵为；
- 稀疏矩阵（Sparse Matrix）：假如矩阵中非零元素个数s相对于总个数t非常小时（一般根据稀疏因子的值判定s<<t）
- 两者的主要==不同是特殊矩阵中特殊元素的分布具有某种规律，而稀疏矩阵中特殊元素（非零元素）的分布没有规律。==


[[CS理论计算机科学/数据结构/数组/对称矩阵]]
##[[CS理论计算机科学/数据结构/数组/三角矩阵]]

##[[CS理论计算机科学/数据结构/数组/对角矩阵]]