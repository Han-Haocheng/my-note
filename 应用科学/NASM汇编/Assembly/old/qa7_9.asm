ASSUME CS:CODESG,SS:STACKSG,DS:DATASG

STACKSG SEGMENT
	DW 0,0,0,0,0,0,0,0
STACKSG ENDS

DATASG SEGMENT
	DB '1. display      '
	DB '2. brows        '
	DB '3. replace      '
	DB '4. modify       '
DATASG ENDS

CODESG SEGMENT
START:
	MOV	AX,STACKSG
	MOV SS,AX
	MOV	SP,10H

	MOV	AX,DATASG
	MOV	DS,AX
	MOV	BX,0

	MOV	CX,4
S0:	PUSH	CX
	MOV	SI,0
	MOV	CX,	4
S1:	MOV	AL,[BX+SI+3]
	AND	AL,11011111B
	MOV	[BX+SI+3],AL
	INC	SI
	LOOP	S1

	ADD	BX,16
	POP	CX
	LOOP	S0

	MOV	AX,4C00H
	INT	21H
CODESG ENDS

END START