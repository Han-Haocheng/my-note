# 完整性约束

自增属性 ``AUTO_INCREMENT``：为整型列设置的属性。需定义主键约束或候选键约束


**完整性约束条件**：用户操作表中的数据时，DBMS 会自动检查该操作是否遵循定义的完整性约束条件
- *实体完整性约束*：`PRIMARY KEY`、`UNIQUE`
	- 候选键：`not null unique`
- *参照完整性约束*：`FOREIGN KEY`
- *用户自定义约束*：`NOT NULL`、`DEFAULT`、`CHECK` 约束等

NULL 约束
- `NULL` 与 `NOT NULL