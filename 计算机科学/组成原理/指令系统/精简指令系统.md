
# 精简指令系统

精简指令集计算机 RISC（Reduced Instruction Set Computer）：用 20%的简单指令组合不常用的 80%的指令功能
- 选用使用频度较高的一些指令，复杂指令的功能由简单指令来组合
- 指令*长度固定*、*格式种类少*、*寻址方式少*
- 只有 `LOAD` / `STORE` 指令访存
- CPU 中有*多个通用寄存器*
- 采用*流水技术*：一个时钟周期内完成一条指令
- 采用*组合逻辑*实现控制器
