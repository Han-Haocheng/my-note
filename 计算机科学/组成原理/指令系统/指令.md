# 指令

**指令字长**：一条指令的总长度，指令字长取决于操作码、操作数地址的长度和操作数地址的个数，是机器字长的整数倍
- 指令字长是机器字长的倍数：半字长指令、单字长指令、双字长指令
- 指令字长会影响取指时间：$机器字长=存储字长=16bit$，则取一条双字长指令需要两次访存操作
- 半字长指令、单字长指令、双字长指令

## 格式
一般格式： `[操作码字段 | 地址码字段]`

### 操作码

**操作码**：反应机器要执行的操作，是指令必不可少的部分
- **操作码位数**：随地址数的减少而增加
- **高地址指令操作码**：每减少一种，最多可构成 $2^4$ 种低一级地址指令
- **操作码出现频率**：高频出现的指令用短操作码，低频出现的指令用长操作码

长度固定RISC：用于指令字长较长的情况，占用更多空间
- 例如：IBM 370，操作码 8 位

长度可变：操作码分散在指令的不同字段中，设计麻烦

按操作数地址数目

| 类型  | 格式                                                                                            | 操作                                                    | 访存次数 | 寻址范围         | 备注                       |
| --- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ---- | ------------ | ------------------------ |
| 零址  | $\|\overset{32}{OP}\|$                                                                        | `OP`                                                  | 0    |              | 无地址码                     |
| 一址  | $\|\overset{8}{OP}\|\overset{24}{A_1}\|$                                                      | `(ACC)OP(A1)` $\to$ `ACC`<br>`(ACC)OP(A1)` $\to$ `A1` | 2    | $2^{24}=16M$ | 用 ACC 替代 $A_1$ 或 $A_2$   |
| 二址  | $\|\overset{8}{OP}\|\overset{12}{A_1}\|\overset{12}{A_2}\|$                                   | `(A1)OP(A2)` $\to$ `A1`<br>`(A1)OP(A2)` $\to$ `A2`    | 4    | $2^{12}=4K$  | 用 $A_1$ 或 $A_2$ 代替 $A_3$ |
| 三址  | $\|\overset{8}{OP}\|\overset{8}{A_1}\|\overset{8}{A_2}\|\overset{8}{A_3}\|$                   | `(A1)OP(A2)` $\to$ `A3`                               | 4    | $2^8=256$    | 用 PC 代替 $A_4$            |
| 四址  | $\|\overset{8}{OP}\|\overset{6}{A_1}\|\overset{6}{A_2}\|\overset{6}{A_3}\|\overset{6}{A_4}\|$ | `(A1)OP(A2)`$\to$` A3`                                | 4    | $2^6=64$     |                          |

按指令字长分类：固定字长、可变字长

### 地址码

**操作数**（地址码）：指出操作数据、寄存器编号、主存地址等信息，其数量由设计者规定

设指令字长为 32 位，操作码固定为 8 位
- 操作指令`OP`
- 第一（目的）操作数地址 $A_1$
- 第二（源）操作数地址 $A_2$
- 结果地址$A_3$
- 下条指令地址$A_4$

### 扩展操作码

扩展操作码技术：多地址指令留下几个 bit 为少地址指令做扩展，减少机器识别指令的时间，操作码越长，识别时间越多
- 多地址每留下 1 位，少地址指令就多 $2$ 倍

- 4 位操作码，最多 15 条三地址指令

|OP| $A_1$ | $A_2$ | $A_3$ |
|-|-|-|-|
|0000| $A_1$ | $A_2$ | $A_3$ |
|0001| $A_1$ | $A_2$ | $A_3$ |
|... |...|...|...|
|1110| $A_1$ | $A_2$ | $A_3$ |

- 8 位操作码，最多 15 条二地址指令

|OP| $A_1$ | $A_2$ | $A_3$ |
|-|-|-|-|
|1111|0000| $A_2$ | $A_3$ |
|1111|0001| $A_2$ | $A_3$ |
|... |...|...|...|
|1111|1110| $A_2$ | $A_3$ |

- 12 位操作码，最多 15 条一地址指令

|OP| $A_1$ | $A_2$ | $A_3$ |
|-|-|-|-|
|1111|1111|0000| $A_3$ |
|1111|1111|0001| $A_3$ |
|... |...|...|...|
|1111|1111|1110| $A_3$ |

16 位操作码，16 条零地址指令

| OP   | $A_1$ | $A_2$ | $A_3$ |
| ---- | ----- | ----- | ----- |
| 1111 | 1111  | 1111  | 0000  |
| 1111 | 1111  | 1111  | 0001  |
| ...  | ...   | ...   | ...   |
| 1111 | 1111  | 1111  | 1110  |

### 通过硬件替代

当用一些硬件资源替代指令字中的地址码字段后

- 可扩大指令的寻址范围
- 可缩短指令字长
- 可减少访存次数

当指令的地址字段为寄存器

- 可缩短指令字长
- 指令执行阶段不访存



## 寻址方式



寻址方式：确定本条指令的操作数地址，下一条要执行指令的地址

- 指令寻址
- 数据寻址
### 指令寻址


**顺序寻址**：通过 `(PC)+1->PC` 作为下一条指令地址
- 其中 `+1` 操作根据实际情况的不同而不同

**跳跃寻址**：由转移指令指出
- 例如 `JMP` 指令

![20240301093910.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301093910.png)

### 数据寻址

`|操作码|寻址特征|形式地址A|`

- 形式地址：指令字中的地址
- 有效地址：操作数的真实地址

约定：指令字长=存储字长=机器字长

#### 立即寻址

形式地址 A 就是操作数

`|OP|#|A|`

- \#：立即寻址特征
- A：立即数，可正可负、补码

特点

- 执行时不需要再次访存
- A 的位限制了立即数的范围

#### 直接寻址

形式地址就是有效地址 `EA=A`

![20240301094827.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301094827.png)

特点

- 需要访问一次存储器
- A 的位数限制该指令操作数的寻址范围
- 操作数地址不易修改，必须修改 A

#### 隐含寻址

操作数地址隐含在操作码中，可减少指令字段长度

![20240301095338.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301095338.png)

如 8086 指令集

- `MUL` 指令：被乘数隐含在 AX（16 位）或 AL（8 中
- `MOVS` 指令：源操作数的地址隐含在 SI 中，目的的操作数隐含在 DI 中

#### 间接寻址

操作数地址保存在形式地址所保存的地址中 `EA=(A)`，有效地址由形式地址间接提供
![20240301100114.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301100114.png)

- 执行指令阶段 2 次访存
- 可扩大寻址范围
- 便于编址程序
![20240301100658.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301100658.png)
- 多次访存

例如
![20240301100953.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301100953.png)
![20240301101112.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301101112.png)

#### 寄存器寻址

有效地址是寄存器编号，寄存器直接寻址（EA=R_i）
![20240301101331.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301101331.png)

- 执行阶段补访存，只访问寄存器，执行速度块
- 寄存器个数有限，可缩短指令字长

#### 寄存器间接寻址

操作数保存在内存单元，操作上的地址保存在寄存器中（`EA=(R_i)`）

![20240301101714.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301101714.png)

- 执行阶段只需要进行一次访存
- 便于编制循环程序

#### 基址寻址

`EA=(BR)+A`

- 可扩大寻址范围
- 有利于多道程序

采用专用寄存器作基址寄存器
- BR 为基址寄存器
- 内容由操作系统或管理程序确定
- BR 内容不变，只能修改形式地址 A

![20240301102128.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301102128.png)



采用通用寄存器作基址寄存器
- R_0 作为基址寄存器
- 用户确定作为基址的寄存器
- 基址寄存器的内容由操作系统决定
- R_0 内容不变，形式地址 A 可变

![20240301102459.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301102459.png)

#### 变址寻址

`EA=(IX)+A`，IX 为变址寄存器(专用或通用)
![20240301102928.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301102928.png)

- 可扩大寻址范围
- IX 的内容由用户给定
- 在程序 执行过程中 IX 内容可变，形式地址 A 不变
- 便于处理数组问题

#### 相对寻址

`EA=(PC)+A`
A 是相对于当前指令的位移量（可正可负，补码）

![20240301104322.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301104322.png)

- A 的位数决定操作数的寻址范围
- 程序浮动，广泛用于转移指令

#### 堆栈寻址

##### 堆栈的特点

堆栈

- 硬堆栈：多个寄存器
- 软堆栈：指定的存储空间

先进后出（一个入出口）栈顶地址由 SP 指出

##### 堆栈寻址举例

- 进栈：`(SP)-1->SP`
![20240301105539.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301105539.png)
- 出栈：`(SP)+1->SP`
![20240301105551.png](计算机科学/组成原理/attachments/Pasted%20image%2020240301105551.png)

##### SP 的修改与主存编址方法有关

- 按字编址：sp 每次修改 1
- 按字节编址：SP 每次修改 存储字长的字节数

> 思考：采用如此多的寻址方式，应该是为了应对不同类型的程序运行，不同的寻址方式的特点和适用范围不同


## 取数指令

取指令
1. `PC` 将保存的指令地址发送给 `MAR`
2. `MAR` 激活存储体地址
3. 存储体将地址所保存的指令数据返回 `MDR`
4. 取 `MDR` 指令数据到 IR中

分析指令
1. 将 `IR` 中保存的数据中的操作码部分传递给 `CU`
2. 将 `IR` 中保存的数据中的地址码部分传递给 `MAR`

执行指令
1. `MAR` 激活存储体对应地址码的数据，返回至 `MDR`
2. 将 `MDR` 中的数据传递给 `ACC`

## 存数指令

取指令
1. `PC` 将保存的指令地址发送给 `MAR`
2. `MAR` 获取存储体对应地址的数据
3. 存储体将对应数据返回至 `MDR`
4. 取 `MDR` 指令至 `IR` 中

分析指令
1. 将 `IR` 中保存的数据中的操作码部分传递给 `CU`
2. 将 `IR` 中保存的数据中的地址码部分传递给 `MAR`

执行指令
1. `ACC` 将数据传递给 `MDR`
2. 将 `IR` 指令数据中的地址码部分的存储单元开辟出来
3. 将 `MDR` 中的数据写入存储体开辟出来的位置

## 程序运行过程

以计算 $ax^2+bx+c$ 举例

1. 将程序通过输入设备送至计算机
2. 程序首地址 -> `PC`
3. 启动程序运行
4. 取数 a
	1. 取指令：`PC`-> `MAR`-> `M`-> `MDR`-> `IR` ; $[PC]+1$ -> `PC`
	2. 分析指令：$[IR]_{OP}$ -> `CU`
		- $OP$：指令部分
	3. 执行指令：$[IR]_{AD}$ -> `MAR` -> `M` -> `MDR` -> `ACC`
		- $AD$：地址码部分
		- `MAR` / `MDR`：类似于接口寄存器
5. 乘以 x
6. 加 b
7. 乘以 x
8. 加 c
9. 打印结果
10. 停机
