# 虚拟内存

辅助存储器运行特征
- 一次性：必须一次全部装入内存，可能会超出内存容量，每次只能装入少量作业
- 驻留性：装入内存后，会一直存在，直到结束运行

虚拟内存：将主存和辅存的地址统一编址，用于主存与辅存交换

## 页式虚拟存储结构

页式虚拟存储结构：以页为单位的虚拟存储结构，将内存按页划分
- 分页方式：基本分页、请求分页

基本分页：程序运行前，所有的页都被一次性装入内存

请求分页：程序运行时，只将当前需要访问的页加载到内存

页框：物理地址按照页的大小划分的物理存储区

