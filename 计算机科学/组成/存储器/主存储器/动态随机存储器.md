# 动态随机存储器


动态存储器 DRAM：需要刷新，行列地址复用，有行缓冲区

动态 RAM 刷新原因：由于电容所保存的数据会随时间流失，因此需要不断进行刷新
刷新与行地址有关：每一次刷新操作，刷新的是一行的所有单元保存的数据。
- 这一行每一列单元所保存的数据放置到每一列读数据线上
- 经过读数据线和写数据线之间的刷新放大器，可以将数据恢复


行列地址复用：先传入行地址，再传入列地址，行列地址从同样的引脚


## 结构

### 单管动态

![[4.2.3image6.png|image.png]]

单管动态：字线通过控制 $T$ 是否导通选择对应数据，检测或控制数据线完成读写操作




![[4.2.3image7.png]]
单管动态 RAM 4116（16 K×1 位）外特性
- 需要 14 根地址线，分两次输入




![[4.2.3image8.png]]

单管动态 RAM 4116（16 K×1 位）


### 三管动态


![image.png](4.2.3image4.png)

三管动态：读出与原存信息相反，写入与输入信息相同
- 预充电有效，$T_4开$
- 假如 读选择线有效， $T_2开$
    - 假如 $C_g$ 未充电，则存储为 0，$T_1$ 关，$V_{DD}\to T_4\to 读数据线$，读数据线为高电平
    - 假如 $C_g$ 充电，存储为 1，$T_1$ 开，$V_DD\to T_4\to T_2\to T_1\to 地$，读数据线为低电平
- 假如 写选择线有效，则 $T_3$ 开，$写数据线\to C_g$


三管动态 RAM 芯片（Intel 1103）
- **刷新放大器**：为了**刷新电容**所保存的数据
- **行地址译码器**：既**选择地址**也**控制读写**

![image.png](4.2.3image5.png)


## 刷新方式

### 分散刷新

![image.png](4.2.3image10.png)

假设：存取周期为 1μs，以 128×128 为例，存取周期 $0.5\mu s$，刷新占用一个存取周期

- 执行操作的周期$t_M$
- 按顺序刷新内存的周期$t_R$
- 一次存储周期 $t_C=t_M+t_R$
- 按照以上方法，在一个电容刷新内会产生过多次数的刷新

### 集中刷新

![image.png](4.2.3image9.png)

假设：存取周期位 0.5μs，单元电容刷新为 2 ms（芯片内部所有电容需要在 2 ms 中刷新）
- 2 ms 是 $2ms\div0.5us=4000t$ 存储周期
- 其中刷新操作（死区）共需要 $128tx0.5μs=64μs$
- 读写或维持操作可执行 $4000t-128t=3872t=1936μs$
- $死时间率=128t\div4000×100\%=3.2\%$


### 异步刷新

![image.png](4.2.3image11.png)
异步刷新

假设 存取周期为 0.5μs，以 128×128 为例
- 若每隔 15.6μs 刷新一行
- 每隔 2 ms 刷新一次
