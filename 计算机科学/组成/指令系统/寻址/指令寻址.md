# 指令寻址


## 顺序寻址

**顺序寻址**：通过 `(PC)+1->PC` 作为下一条指令地址
- 其中 `+1` 操作根据实际情况的不同而不同


## 跳跃寻址

![[Pasted image 20240301093910.png]]

**跳跃寻址**：由转移指令指出，改变 PC 的值
- 例如 `JMP` 指令
- 绝对地址：指令直接将操作码赋值给 `PC` , `(A1)->(PC)`
- 相对地址：指令操作码与 `PC` 相加后再赋值给 `PC`，`(PC)+(A1)->(PC)`

注意：发生跳跃寻址后需要再次执行PC+1->PC
