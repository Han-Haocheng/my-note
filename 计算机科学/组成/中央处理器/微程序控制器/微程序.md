# 微程序


微程序：一条机器指令，包含多个微指令

微指令：一个微指令包含多个微操作，完成一次

微操作：向某部件发送一次控制信号

完成一条机器指令对应一段微程序，需要 n 条微操作命令，几个微操作可以组合成一条由高低电平组成的微指令，每个高电平对应一个微操作
- 对应一个微程序，一个微程序包含多个
- 将这些高低电平保存在只读存储器 ROM（存储逻辑）


![[Pasted image 20240307162934.png|20240307162934.png]]

