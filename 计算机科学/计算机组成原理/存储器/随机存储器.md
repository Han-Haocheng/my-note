
# 随机存储器

SRAM 和 DRAM 的对比


| 特点    | SRAM | DRAM |
| ----- | ---- | ---- |
| 存储信息  | 触发器  | 电容   |
| 破坏性读出 | 非    | 是    |
| 需要刷新  | 不要   | 需要   |
| 送行列地址 | 同时送  | 分两次送 |
| 运行速度  | 快    | 慢    |
| 集成度   | 低    | 高    |
| 存储成本  | 高    | 低    |
| 主要用途  | 高速缓存 | 主机内存 |


## 静态存储器SRAM


![[计算机科学/计算机组成原理/存储器/Pasted image 20260312211437.png]]


静态存储器 SRAM（易失性存储器）：存集成度低，读写快，价格高，通常用作 Cache、TLB

## 动态存储器


动态存储器 DRAM（）：需要刷新，行列地址复用，有行缓冲区

动态 RAM 刷新原因：由于电容所保存的数据会随时间流失，因此需要不断进行刷新
刷新与行地址有关：每一次刷新操作，刷新的是一行的所有单元保存的数据。
- 这一行每一列单元所保存的数据放置到每一列读数据线上
- 经过读数据线和写数据线之间的刷新放大器，可以将数据恢复


行列地址复用：先传入行地址，再传入列地址，行列地址从同样的引脚


### 分散刷新

![image.png](计算机科学/计算机组成原理/attachments/4.2.3image10.png)

假设：存取周期为 1μs，以 128×128 为例，存取周期 $0.5\mu s$，刷新占用一个存取周期

- 执行操作的周期$t_M$
- 按顺序刷新内存的周期$t_R$
- 一次存储周期 $t_C=t_M+t_R$
- 按照以上方法，在一个电容刷新内会产生过多次数的刷新

### 集中刷新

![image.png](计算机科学/计算机组成原理/attachments/4.2.3image9.png)

假设：存取周期位 0.5μs，单元电容刷新为 2 ms（芯片内部所有电容需要在 2 ms 中刷新）
- 2 ms 是 $2ms\div0.5us=4000t$ 存储周期
- 其中刷新操作（死区）共需要 $128tx0.5μs=64μs$
- 读写或维持操作可执行 $4000t-128t=3872t=1936μs$
- $死时间率=128t\div4000×100\%=3.2\%$


### 异步刷新

![image.png](计算机科学/计算机组成原理/attachments/4.2.3image11.png)
异步刷新

假设 存取周期为 0.5μs，以 128×128 为例
- 若每隔 15.6μs 刷新一行
- 每隔 2 ms 刷新一次


## 译码结构

### 单译码结构
![[计算机科学/计算机组成原理/存储器/Pasted image 20260312210949.png]]

单译码结构：N 位地址对应 $2^n$ 根译码线


## 双译码结构

![[计算机科学/计算机组成原理/存储器/Pasted image 20260312211719.png]]

双译码结构：N 位地址对应 $2^{\frac{n}{2}+1}$ 根译码线

![[2026-03-12_212008.svg]]


