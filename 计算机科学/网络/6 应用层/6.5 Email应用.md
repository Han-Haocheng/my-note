# Email应用

## Email应用构成
![[计算机科学/网络/6 应用层/attachments/Pasted image 20251021135840.png]]


#计算机/网络/应用层/Email Email应用构成组件：邮件客户端、邮件服务器、SMTP协议
- 邮件客户端user agent：读写Email消息，与服务器交互，收发Email消息，Web客户端
- 邮件服务器Mail Server：存储并等待发送给用户的Email，邮箱，消息队列message queue

## SMTP

简单邮件传输协议SMTP（Simple Mail Transfer Protocol）：邮件服务器之间传递消息所使用的协议。使用TCP进行email可靠传输，通常端口为==25==，命令响应模式，持久性连接
- 传输过程：握手->消息传输->关闭
- 命令command：格式为7位ASCII码文本，HELO、MAIL FROM、RCPT TO、DATA、QUIT
- 响应response：格式为状态代码和语句
- 结束标志：`CRLF.CRLF`

```smtp
S: 220 hamburger.edu
C: HELO crepes.fr
S: 250 Hello crepes.fr, pleased to meet you
C: MALL FROM: <alice@crepes.fr>
S: 250 alice@crepes.fr... Sender ok
C: RCPT TO: <bob@hamburger.edu>
S: 250 bob@hamburger.edu ... Recipient ok
C: DATA
S: 354 Enter mail, end with "." on a line by itself
C: Do you like ketchup?
C: How about pickles?
C: .
S: 250 Message accepted for delivery
C: QUIT
S: 221 hamburger.edu closing connection
```
## Email消息格式

![[计算机科学/网络/6 应用层/attachments/Pasted image 20251021160627.png]]

Email消息格式^[RFC-822]

- 头部行header：To、From、Subject
- 消息体body：ASCII字符消息本身


多媒体邮件扩展MIME：在邮件头部增加额外的行，声明内容类型^[RFC-2045,RFC-2056]


```smtp
From: alice@crepes.fr
To: bob@hamburger.edu
Subject: Pocture of yummy crepe.
MIME-Version: 1.0
Content-Transfer-Encoding: base64
Content-Type: image/jpeg

base64 encoded data ......
..........................
...... base64 encoded data
```
## 邮件访问协议

邮件访问协议：从服务器获取邮件

- POP（Post Office Protocol）：认证授权和下载^[RFC-1939]
- IMAP（Internet Mail Access Protocol）：更多功能，更加复杂，能够操纵服务器上存储的消息^[RFC-1730]
- HTTP

### POP协议

POP3：无状态协议

认证过程
- 客户端命令：`User`声明用户名、`Pass`声明密码
- 服务器响应：`+OK`、`-ERR`

事物阶段
- `List`：列出消息数量
- `Retr`：用编号获取消息
- `Dele`：删除消息
- `Quit`：退出认证

模式
- 下载并删除模式：如果更换客户端，无法重读该邮件
- 下载并保持模式：不同客户端都可以保留消息的拷贝

### IMAP协议

IMAP：所有消息统一保存在服务器，允许用户利用文件夹组织消息，支持跨会话的用户状态
- 跨会话用户状态：文件夹名、文件夹与消息ID之间的映射

