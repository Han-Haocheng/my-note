# 文件使用磁盘的实现


## 通过文件使用磁盘

![[计算机科学/操作系统原理/attachments/2025-09-22_162925.svg]]
- 用open访问数据，核心是inode

fs/read_write.c

```c
int sys_write(int fd,const char*buf,int count){
	struct file *file = current->filp[fd];
	struct m_inode*inode=file->inode;
	if(S_ISREG(inode->i_mode))
		return file_write(inode,file,buf,count);
}
```

```c
int file_write(struct m_inode*inode,struct file*filp,char*buf,int count){
	off_t pos;
	if(filp->f_flags&O_APPEND)
		pos=inode->i_size;
	else
		pos = filp->f_pos
	while(i<count){
		block=create_block(inode,pos/BLOCK_SIZE);//创建block缓冲
		bh=bread(inode->i_dev,block);     //发出请求
		int c = pos%BLOCK_SIZE;           
		char*p=c+bh->b_data;
		bh->b_dirt=1;
		c=BLOCK_SIZE-c;
		pos+=c;
		...
		while(c-->0)*(p++)=get_fs_byte(buf++);
		brelse(bh);
	}
	filp->f_pos=pos
}

struct d_inode{
	unsigned short i_mode;
	...
	unsigned short i_zone[9];
}

// (0-6):直接数据块
// (7):一重间接
// (8):二重间接

int _bmap(m_inode*inod,int block,int create){
	if(block<7){
		if(create&&!inode->i_zone[block]){
			inode->i_zone[block]=new_block(inode->dev);
			inode->i_ctime=CURRENT_TIME;
			inode->i_dirt=1;
		}
		return inode->i_zone[block];
	}
	block-=7;
	if(block<512){
		bh=bread(inode->i_dev,inode->i_zone[7]);
		return (bh->b_data)[block];
	}
	...
}
```

1. 获取字符位置：file中一个读写指针，是开始地址（fseek所修改的指针)，再加上count
2. 寻找盘块号：inode的作用
3. 用盘块号、buf等形成request放入磁盘读写队列

## 设备文件的inode


```c
struct m_inode{
	unsigned short i_mode;
	...
	unsigned hsort i_zone[9];
	struct task_struct *i_wait;
	unsigned shrot i_count;
	unsigned char i_lock;
	unsigned char i_dirt;
	...
}

int sys_open(cout char* filename,int flag){
	if(S_ISCHR(inode->i_mode)){
		if(MAJOR(inode->i_zone[0])==4){
			current->tty=MINOR(inode->i_zone[0]);
		}
	}
}
#define MAJOR(a)(((unsigned)(a))>>8)
#define MINOR(a)((a)&0xff)
```