# 内存换入

![[计算机/操作系统原理/attachments/2025-09-09_155955.svg]]

![[计算机/操作系统原理/attachments/2025-09-10_172320.svg]]

虚拟内存可能远大于物理内存，操作系统通过内存换入将数据从虚拟内存映射到物理内存
![[计算机/操作系统原理/attachments/Pasted image 20250910172012.png]]

内存换入：从虚拟内存的数据映射到物理内存中，从而实现更大的内存使用

## 请求调页
![[计算机/操作系统原理/attachments/2025-09-14_132532.svg]]


## 代码实现

### 设置中断处理程序

| 中断号 | 名称                  | 说明         |
| --- | ------------------- | ---------- |
| 12  | Segment not Present | 描述符所指的段不存在 |
| 14  | Page fault          | 页面不存在      |
```c
void trap_init(void){
	set_trap_gate(14,$page_fault);
}

#default set_trap_gate(n,addr)\
	_set_gate(&idt[n],15,0,addr)
	

```


### 页错误中断处理程序

mm/page.s

```
.globl _page_fault
	chgl %eax,(%esp)
	pushl %ecx
	pushl %edx
	push %ds
	push %es
	push %fs
	
	movl %0x10,%eax
	mov %dx,%ds
	mov %dx,%es
	mov dx,%fs
	movl %cr2,%edx ! 页错误线性地址
	
	pushl %edx
	pushl %eax     ! 压入参数
	testl $1,%eax
	jne 1f
	call _do_no_page! 调用函数
	jmp 2f
1:call _do_wp_page
2:add $8,%esp
	pop %fs
	pop %es
	pop %ds
	pop %edx
	pop %ecx
	pop %eax
	iret
```


### 处理页错误

mm/memory.c

```c
void do_no_page(unsigned long error_code,unsigned long address)  
{  
    int nr[4];  
    unsigned long tmp;  
    unsigned long page;  
    int block,i;  
  
    address &= 0xfffff000;  // 页面地址
    tmp = address - current->start_code;  // 页面对应的偏移
    if (!current->executable || tmp >= current->end_data) {  
       get_empty_page(address);   
       return;  
    }  
    if (share_page(tmp))  
       return;  
    if (!(page = get_free_page()))  // 获取物理空闲页
       oom();  
/* remember that 1 block is used for header */  
    block = 1 + tmp/BLOCK_SIZE;  
    for (i=0 ; i<4 ; block++,i++)  
       nr[i] = bmap(current->executable,block);  
    bread_page(page,current->executable->i_dev,nr);  // 从磁盘中读取一个页
    i = tmp + 4096 - current->end_data;  
    tmp = page + 4096;  
    while (i-- > 0) {  
       tmp--;  
       *(char *)tmp = 0;  
    }  
    if (put_page(page,address))  // 添加页
       return;  
    free_page(page);  
    oom();  
}
```

### 放置新页

mm/memory.c
```c
unsigned long put_page(unsigned long page,unsigned long address)
{
	unsigned long tmp, *page_table;

/* NOTE !!! This uses the fact that _pg_dir=0 */

	if (page < LOW_MEM || page >= HIGH_MEMORY)
		printk("Trying to put page %p at %p\n",page,address);
	if (mem_map[(page-LOW_MEM)>>12] != 1)
		printk("mem_map disagrees with %p at %p\n",page,address);
	page_table = (unsigned long *) ((address>>20) & 0xffc);
	if ((*page_table)&1)
		page_table = (unsigned long *) (0xfffff000 & *page_table);
	else {
		if (!(tmp=get_free_page()))
			return 0;
		*page_table = tmp|7;
		page_table = (unsigned long *) tmp;
	}
	page_table[(address>>12) & 0x3ff] = page | 7; // 在页表中建立页映射
/* no need for invalidate */
	return page;
}
```