# 5.4 软件体系结构


![[计算机/软件工程/attachments/Pasted image 20251021095321.png]]
## 程序结构

![[计算机/软件工程/attachments/Pasted image 20251021095400.png]]

- 深度：件结构中控制的层数
- 宽度：软件结构内同一个层次上的模块总数的最大值
- 扇出：一个模块直接控制(调用)的模块数目
- 扇入：一个模块被多少个上级模块直接调用的数目

## 数据结构


标量项(scalar item)：表示一个用标识符标识的单元信息元素1在存储器中可以用一个确定的单一地址来访问它。
- 所有数据结构中最简单的一种、其规模和格式可以在一种编程语言所确定的边界内有所变化
- 顺序向量：标量项作为一组或连接成组时，形成顺序向量


n维空间：把顺序向量扩展为多维，就构成了一个n维空间。在大多数编程语言中，n维空间被称为数组。

标量、向量和空间可以用不同的格式组织。链表(linked list)就是一种非邻接的标量的组织。

向量或空间可以用一定的方式组织呈成表结构。每个节点具有适当的组织和一个或多个指针，这个指针表明表中下一个节点在节点存储器中的地址

