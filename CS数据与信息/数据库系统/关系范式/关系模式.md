# 关系模式

关系模式：`R(U,D,DOM,F)`，用来定义关系


- 简化表示 `R(U, F)`
- 当且仅当U上的一个关系r满足F时，r称为关系模式`R(U, F)`的一个关系

## 组成

- 关系名 R
- 属性名集合 U：组成该关系的属性名集合       
- 属性域 D：属性组 U 中属性所来自的域
- 映象集合 DOM：属性向域的映象集合
- 依赖关系集合 F：属性间数据的依赖关系集合。即限定了组成关系的各个元组必须满足的完整性约束条件。