
码片序列: 每个信号源（发送设备）都有一个独特的码片序列，通常表示为一个由-1 和 1 组成的二进制序列，如(-1, 1, -1...)。这个码片序列用于对信号进行编码。
信号传输: 在发送时，信号源将原始数据与其独特的码片序列相乘，然后将编码后的信号在同一个频谱上发送出去。由于每个信号源使用不同的码片序列，即使所有信号都在同一个信道上传输，它们之间不会互相干扰。
信号接收: 在接收端，目的站（接收设备）通过已知的源站的码片序列来从复合信号中提取所需的信号。具体方法是将接收到的信号与已知的码片序列做内积（即乘积的和）。

# 码分多路复用

 码分复用CDM(Code Division Multiplexing)：每一个用户可以在同样的时间使用*同样频带*进行通信，各用户使用经过特殊挑选的不同*码片序列chipping sequence*，因此各用户之间 不会造成干扰
- 码分多址CDMA(Code Division Multiple Access)
- 有很强的抗干扰能 力，其频谱类似于白噪声，不易被敌人发现
-  码片chip：CDMA 中，每一个比特时间划分为 m 个短的间隔

## 计算

 计算：$编码信号=原始数据\times 码片序列$
- 发送bit 1：发送自己的码片序列
- 发送bit 0：发送自己码片序列的反码

## 特点
### 码片序列 特点

- 每一个站分配的码片序列不仅必须各不相同
- 各用户吗片序列互相*正交orthogonal*

$$\frac{1}{m}S_{i}\cdot S_{j}=\begin{cases}1, & i=j \\0, & i\neq j\end{cases}\qquad\frac{1}{m}S_{i}\cdot \overline{S_{j}}=\begin{cases}-1, & i=j \\0, & i\neq j\end{cases}$$

 编码：令$\{ d_{i} \}$为原始数据序列，各个用户的叠加向量为 $$P =\sum_{i=1}^{N}d_{i}\cdot S_{i}=\sum_{i=1}^{N}\overset{(-)}{S_{i}}$$

 解码：码片序列与编码信号的内积

$$\frac{1}{m}S_{i}\cdot P=\begin{cases}1, & S_{i}\in P\\-1,  & \overline{S_{i}}\in P\\0,  & S_{i},\overline{S_{i}} \in P\end{cases}$$



### 编码特点

- 向量 S 和 T 的 规格化内积(inner product)都是 0
$$S\cdot T=\frac{1}{m}\sum_{i=1}^{m}S_{i}T_{i}=0$$
- 任何一个码片向量和该码片向量自己的规格化内积都是 1
$$S\cdot S=\frac{1}{m}\sum_{i=1}^{m}S_{i}S_{i}=\frac{1}{m}\sum_{i=1}^{m}S^{2}_{i}=\frac{1}{m}\sum_{i=1}^{m}(\pm1)^{2}=1$$

- 而一个码片向量和该码片反码的向量的规格化内积值是 –1

$$
S\cdot S = \frac{1}{m}\sum_{i=1}^{m}S_{i}S_{i}=-1
$$
- 向量 S 和各站码片反码的向量的内积也是 0


![[2025-10-14_141731.svg]]



 扩频通信(spread spectrum)：现假定 S 站要发送信息的数据率为 b bit/s。由于每一个比特要转换成 m 个比特的码 片，因此 S 站实际上发送的数据率提高到 mb bit/s，同时 S 站所占用的频带宽度也提高到原 来数值的 m 倍
- 直接序列扩频 DSSS (Direct Sequence Spread Spectrum)：码片序 列就是这一类。
- 跳频扩频 FHSS (Frequency Hopping Spread Spectrum)