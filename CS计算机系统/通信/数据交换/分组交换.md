---
tags:
  - 计算机科学/计算机网络
---

# 分组交换

![[2025-10-14_130924.svg]]

分组交换(存储转发)：采用存储转发技术，把一个报文划分为几个分组后再进行传送，到达目标后再重组为报文
- 报文(message)：要发送的整块数据
- 分组(包、packet)：由数据段和首部组成，==互联网中传送的数据单元==
- 首部(包头、header)：在每一个数据段前面，加上一些由必要的控制信息
- 顺序：发送是顺序、接收是乱序


## 方式

| 对比的方面 | 数据报服务 | 虚电路服务 |
| --- | --- | --- |
| 思路 | 可靠通信应当由用户主机来保证 | 可靠通信应当由网络来保证 |
| 连接的建立 | 不需要 | 必须有 |
| 终点地址 | 每个分组都有终点的完整地址 | 仅在连接建立阶段使用,每个分组使用短的虚电路号 |
| 分组的转发 | 每个分组独立选择路径进行转发 | 属于同一条虚电路的分组均按照同一路由进行转发 |
| 当节点出现故障时 | 出故障的节点可能会丢失分组,一些路由可能会发生变化 | 所有通过故障的节点的虚电路均不能工作 |
| 分组的顺序 | 到达终点时不一定按发送顺序 | 总是按发送顺序到达终点 |
| 端到端的差错处理和流量控制 | 由用户主机负责 | 可以由网络负责,也可以由用户主机负责 |


[[数据报]]


[[虚电路]]

## 特点

- 根据首部中包含的目的地址、源地址等控制信息进行转发
- 每个分组在互联网中==独立选择==传输路径
- 位于网络核心部分的路由器负责转发分组
- 路由器主要创建和动态维护转发表


优点：高效、灵活、迅速、可靠

- 高效：在分组传输的过程中动态分配传输带宽，对通信链路是逐段占用
- 灵活：为每一个分组独立地选择最合适的转发路由
- 迅速：以分组作为传送单位，可以不先建立连接就能向其他主机发送分组
- 可靠：保证可靠性的网络协议；分布式多路由的分组交换网，使网络有很好的生存性

缺点：排队延迟、产生额外开销
- 可能产生拥塞 congestion：分组延迟和丢失，需要协议处理可靠数据传输和拥塞控制

## 流程

1. 暂存收到的分组
2. 检查分组首部
3. 检查转换表
4. 按照首部中的目的地址
5. 找到合适的接口转发出去

## 计算

报文交付时间$T=\frac{M}{R}+\frac{(h-1)L}{R}=\frac{M}{R}+\frac{nL}{R}$
- M：报文 bits
- R：链路宽带（数据传输速率） bps
- L：分组长度（大小）bits
- h：跳步数
- n：路由器数


在下图所示的采用“存储-转发”方式的分组交换网络中，所有链路的数据传输速率为100 Mbps，分组大小为1000B其中分组头大小为20 B。若主机H1向主机H2发送一个大小为980000 B的文件，则在不考虑分组拆装时间和传播延迟的情况下，从H1发送开始到H2接收完为止，需要的时间至少是多少?

![[2025-10-14_154315.svg]]

- 数据传输速率 100 000 000bps
- 分组大小：1 000B
- 报文：980 000B
- 跳步：3
- 注意：字节和bit的区别
$$\frac{(9.8e5+20\times 1000)\times 8}{1e8}+\frac{2\times 8\times 1e3}{1e 8}=80ms+0.16ms=80.16ms$$

