---
tags:
  - 计算机科学/操作系统/内存管理
  - 类型/过程
---

# 装入

**装入**：由装入程序将装入模块装入内存，即构造 PCB，形成进程，插入就绪队列准备运行
## 分类

### 绝对装入

**绝对装入方式**：完全按照目标程序中所给定的地址装入内存，即目标程序中使用的是绝对地址。

### 可重定位装入

**可重定位装入方式**：根据内存目前的使用情况，将装入模块装入到内存的某个位置。由于用户的目标程序地址往往都是从 0 开始的，而程序中的其他地址也往往用相对地址形式表示，因此只能采用可重定位装入方式。我们把装入时对目标程序中的指令地址和数据地址的修改过程称为重定位。
### 动态运行时装入

**动态运行时装入方式**：又称动态重定位，程序在内存中若发生移动，需要这种装入方式，该种方式是动态运行时的装入程序，在把装入模块装入内存后，并不立即把装入模块中的相对地址转换为绝对地址，而是把这种地址转换推迟到程序真正要执行时才进行。因此，装入内存后的所有地址都仍是相对地址。灵活性强，是现代操作系统常用方式。
