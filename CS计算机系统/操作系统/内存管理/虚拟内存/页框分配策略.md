---
tags:
  - 计算机科学/操作系统/内存管理
  - todo/未知识化
---
# 页框分配策略



## 内存分配策略

基于请求分页系统的特点，进程在执行前只调入一部分页面到内存，但是具体给特定进程多大的进程空间，需要考虑以下几点：
### 特点
- 分配给单个进程的存储量越小，驻留在主存中的进程数就越多，从而提高 CPU 的利用率。
- 然而，如若单个进程在主存中的页数过少，则系统访问的缺页率会提高，系统性能会受到影响。
- 如若单个进程页数过多，依据[[局部性原理]]，系统性能应该有所提高。但如果过多给特定的进程分配更多的主存空间对系统的性能没有太多的影响。


## 调页策略

调页策略
何时调入页面:
- 预调页策略。采用以预测为基础的预调页策略，将预计在不久之后便会被访问的页面预先调入内存。
- 请求调页策略。当进程在运行中需要访问的页面不在内存时，则进程将提出调页请求，由系统将所需页面调入内存。


何处调入页面:
- 在请求分页系统中，通常将外存划分为*文件区*和*对换区*两部分，前者用以存放文件，后者用于存放对换页面
- 当系统有足够对换区时，可全部从对换区中调入页面
- 当系统缺少足够的对换区时，凡是没有被修改的文件从文件区调入，否则从对换区调入
- UNIX 方式，凡是未运行过的页面，都从文件区调入；而对于曾经运行过但又被换出的页面，在下次调入时，应从对换区调入。


流程

页面调入过程:
- 当要访问进程页面不在内存时，向 CPU 发出缺页中断。
- 保留 CPU 环境，转入缺页中断处理程序。
- 查找页表获得外存的物理块号。
- 若内存有空白物理块，则调入该页。
- 若内存已满，则按某种置换算法选择一个页面淘汰，调入所缺页。

