# 闲逛进程

闲逛进程（空闲进程）：一种没有明确工作，用于解决调度中可能发生的特殊情况的进程。是操作系统中用于统计 CPU 空闲情况的特殊进程


当就绪队列没有除闲逛进程以外的进程可供调度时，系统就会调用闲逛进程使用处理器，系统会运行闲逛进程，以保证 CPU 持续运行并节能。闲逛进程的优先级是最低的，每当有其他进程进入就绪队列后，就会引起处理机调度。

意义：使 CPU 在等待下一个计算或进程进入时保持运行状态，避免 CPU 停止工作。通过运行闲逛进程，系统可以统计 CPU 的空闲情况，有助于优化资源利用和节能。
