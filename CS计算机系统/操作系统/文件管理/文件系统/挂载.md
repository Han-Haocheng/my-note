# 挂载文件系统

文件系统挂载（挂载）：指将新的文件系统关联至当前根文件系统。
- 文件在使用前必须打开一样，文件系统在进程使用前必须先安装
- 可以使用 mount 命令在目录树中的指定目录（挂载点）附加文件系统。
- 挂载点作为要挂载文件系统的访问入口，必须已经存在，一般是不会被进程使用到的目录。
- 挂载后，挂载点下原有文件将会被临时隐藏。
- 卸载，是指将某文件系统与当前根文件系统的关联关系移除。



