# 分区


分区：是将物理硬盘驱动器（HDD）或固态硬盘（SSD）划分为独立存储区域的过程。每个分区可独立访问和管理，且可设置不同文件系统。
分区类型：
- 主分区：硬盘分出的一部分区域，用于存储数据，如 C 盘，一般作为启动分区。
- 拓展分区：剩余部分区域，可进一步划分为逻辑分区。
- 逻辑分区：依赖于拓展分区，用于存储数据。

好处：
- 提高性能：分散数据，减少磁头移动距离。
- 提高可用性和安全性：分区损坏不影响其他分区，敏感数据可单独存储。
- 简化管理和提高灵活性：便于管理不同类型数据，可根据需求调整分区大小。


