# 操作系统



## 进程与程序

- 程序：是*保存在外存中*的*静态*文件
- 进程：是*有生命周期*的*动态*程序
- 程序与进程的关系：一个程序可以对应多个进程

## 操作系统作用

- 控制计算机的硬件和软件资源
	- 控制面板：设置软硬件资源
	- 设备管理器：管理软硬件资源
- 屏蔽软硬件细节，提高用户的工作利用率
- 提高系统资源的*利用率* ~~运行速度~~

## 操作系统特性



[[计算机科学/基础/操作系统/功能]]

[[计算机科学/基础/操作系统/分类]]

## 常用操作系统

### Windows操作系统

Windows：个人电脑操作系统的垄断地位

### UNIX操作系统

UNIX：多用户多任务、分时操作系统，可用于网络服务器

### Linux操作系统

Linux：开源的、多用户、多任务，支持多线程和多CPU的分时操作系统

### MacOS操作系统

MacOS：苹果专用操作系统，基于UNIX，是首个在商用领域成功运用的计算机图形用户界面的操作系统

### 智能手机操作系统

- Android：基于Linux，用于移动设备
- iOS
- WindowsPhone8
- HarmonyOS

### 国产操作系统

- 红旗Linux
- 统信UOS
- 银河麒麟KylinOS

> [!NOTE] 下列软件中属于应用软件的是</br>A. 鸿蒙操作系统</br>B. 微信</br>C. 支付宝</br>D. linux

> [!question] 通过windwos10操作系统开始菜单中的关机按钮的扩展菜单，无法实现以下的那个功能</br>A.关机</br>B.重新启动</br>C.休眠</br>D.更改当前用户密码


关于Windows10的窗口，描述错误的是
- 主要的窗口包括应用程序窗口和文档窗口等
- 窗口的名称显示在标题栏
- 可以同时打开多个窗口，但只有一个窗口是活动的
- 窗口的位置与大小都是固定的

在Windows10操作系统中，关闭应用程序方式正确的有 C
- 按Ctrl+F4
- 按Ctrl+Esc
- 按Alt+F4
- 按shift+F4

下列关于window10的描述中，错误的是 D
- Windows10是一个多任务操作系统，允许多个程序同时运行
- 在某一时刻只能有一个窗口处于活动状态
- 非活动窗口在后台运行
- 非活动窗口可以接收用户的键盘和鼠标输入等操作

可以完成窗口切换的是 Alt+Tab 和 单击要切换窗口的任何可见部位 单机任务栏上要切换的应用程序按钮

- 窗口是应用程序运行后的工作区
- 同时打开很多程序可以重叠
- 窗口的位置可以移动，但大小不能改变
- 窗口的位置和大小都

- 不可以在屏幕上意动
- 可以缩小成任务栏上的一个图标
- 大小可以调整

> [!NOTE] 利用 win10 搜索功能查找文件时，说法正确的是 C</br>A. 要求搜索的文件是文本文件</br>A. 根据日期查找时，必须输入文件的最后修改日期</br>A. 根据文件名查找时，至少需要输入文件名的一部分或通配符</br>A. 用户设置为隐藏的文件，只要符合查找条件，在任何情况下都将被找出
