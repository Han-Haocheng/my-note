# GBN后退N帧协议

![[Pasted image 20260607172328.png]]


GBN协议Go-Back-N：分组头包含k-bit序列号，窗口尺寸为N，最多允许N个分组未确认，采用累积确认机制，为空中的分组设置计时器timer
- 累积确认机制： 收到ACK(n)要求确认到序列号n（包含n）的分组均以被正确接收，会收到重复ACK
- 超时Timeout(n)事件：重传序列号大于等于n，且还未收到ACK的所有分组
- ACK机制：发送拥有最高序列号，且已被正确接收的分组ACK，会产生重复ACK
- 乱序到达的分组：直接丢弃，重新确认序列号最大的按序到达的分组

## 流程


![[Pasted image 20251104140743.png]]


![[Pasted image 20251104141341.png]]

![[Pasted image 20251104142047.png]]

![[Pasted image 20251104142728.png]]

## 特性

为什么 GBN 的发送窗口必须小于等于 $2^n-1$
首先假设接收窗口为 1,发送窗口为 2 n,n 取 3


### 顺序接收

顺序接收：接收方的接收数据是是顺序的

### 累积确认

累积确认：因为顺序接收，所以可以通过对之前收到的所有帧进行确认，收到一个



